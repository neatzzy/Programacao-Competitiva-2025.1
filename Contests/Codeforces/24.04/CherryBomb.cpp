#include <bits/stdc++.h>

using namespace std;

#define vi vector<int>
#define ll long long
#define pb push_back
#define mp make_pair
#define ii pair<int,int>

int main(){
    
    int testes; cin >> testes;

    for(int i = 0 ; i < testes ; i++){
        int n, k; cin >> n >> k;
        vi a;
        for(int j = 0 ; j < n ; j++){
            int x; cin >> x;
            a.pb(x);
        }
        vi b;
        for(int j = 0 ; j < n ; j++){
            int x; cin >> x;
            b.pb(x);
        }
    }

    
    return 0;
}